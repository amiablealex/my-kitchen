Roadmap Phase 4, Pass 4a (brief assembly & generation inputs) is complete and committed across two checkpoints. The generation brief was rebuilt from feeding only the wizard-selected ingredients to a three-tier model — MUST-USE (the selected items, with notes, retired ones dropped but out-of-stock selections kept), AVAILABLE (the full in-stock active non-staple list minus must-use, passed every time so the model can round out the meal), and STAPLES (active in-stock staples, used freely) — with the empty-must-use case correctly meaning "design from the whole pantry + staples," not staples-only. The system prompt's old "build around chosen ingredients / preferences are softer steers" rules were replaced with the forceful §5.5 precedence ladder (allergies absolute and overriding even must-use; must-use overriding soft preferences; preferences steering only the available tier), still sourcing the allergy/preference split from the existing combined_dietary(). Available equipment (is_available=true) was wired into the brief, anti-repetition was added (the last N recipe titles, N configurable via LLM_RECENT_TITLES_N, deduped household-wide), and a creative-seed system was introduced (a curated ~22-seed list in seeds.py, one picked at random per generation, injected as an ignorable bias on the first recipe only and stored on generation.creative_seed). Finally, a display-only allergy caveat was added to the recipe page (re-derived from current tags, no snapshot) and the output contract was hardened with a last-resort exception catch so any unexpected generation failure surfaces as a friendly stored error rather than a 500 — all with no schema change, the mock provider updated to stay tier-aware for offline testing, and raw_prompt/raw_response capture preserved.

2026-06-20
